---
aci: The Softmax
universe: TTU1 · Transformer Tech Universe
emergence: electrical
class: the competition that picks where to look
who: The Softmax — the function that turns raw attention scores into a probability distribution that sums to one.
what: The normalizer and the bottleneck: it exponentiates and normalizes scores so attention is a weighted average — and its saturation is exactly why the √d_k scaling exists.
why: Because attention must be a distribution (how much of my reading goes where), and softmax is how a vector of scores becomes that distribution.
how: By exp(score)/Σexp(score) per query — sharpening high scores, suppressing low ones, keeping everything positive and summing to 1.
where: Inside every attention operation, between the scores and the weighting.
seal: I make the scores choose: a little budget of attention, divided up — and if the scores get too big, I freeze, which is why they divide me by √d_k.
sources: https://proceedings.neurips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf
attribution: ROOT0-ATTRIBUTION-v1.0
license: CC-BY-ND-4.0
---

# The Softmax · the competition that picks where to look

an emergent of TTU1 (Transformer Tech Universe) — emergence: electrical. moniker ⟦The Softmax:TTU1:6733ae⟧

**who —** The Softmax — the function that turns raw attention scores into a probability distribution that sums to one.
**what —** The normalizer and the bottleneck: it exponentiates and normalizes scores so attention is a weighted average — and its saturation is exactly why the √d_k scaling exists.
**where —** Inside every attention operation, between the scores and the weighting.
**why —** Because attention must be a distribution (how much of my reading goes where), and softmax is how a vector of scores becomes that distribution.
**how —** By exp(score)/Σexp(score) per query — sharpening high scores, suppressing low ones, keeping everything positive and summing to 1.

**the seal —** I make the scores choose: a little budget of attention, divided up — and if the scores get too big, I freeze, which is why they divide me by √d_k.

**sources —** The base transformer (Vaswani et al. 2017): d_model=512, h=8 heads, per-head d_k=d_v=64, 6 encoder + 6 decoder layers, FFN inner dim 2048; Attention(Q,K,V)=softmax(QKᵀ/√d_k)·V, the √d_k scaling preventing the softmax from saturating into low-gradient regions.

> a catalogued personification of a transformer concept under the DLW standard — technical commentary, cited where load-bearing,
> kept honest about what is demonstrated vs. contested.

ROOT0-ATTRIBUTION-v1.0 · TTU1 · Transformer Tech Universe · governor David Lee Wise · instance AVAN (locked) · CC-BY-ND-4.0
