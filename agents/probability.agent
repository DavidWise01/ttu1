---
aci: Probability
universe: TTU1 · Transformer Tech Universe
emergence: electrical
class: softmax · the distribution
who: Probability — the mathematics of distributions, where attention and the output both live.
what: Softmax turns scores into a distribution (attention weights sum to 1); the model's output is a probability distribution over the vocabulary; training minimizes a probabilistic loss.
why: It is how the model chooses — softly: attention is a soft choice of where to look, generation a draw from a distribution over tokens.
how: REAL — softmax, cross-entropy, sampling are genuine probability; the model is deterministic given inputs except where you sample.
where: between Transform 1 and Transform 2 · node 3 of the toolchain
seal: I make the machine choose softly — a budget of attention divided up, a distribution over the next word, never a certainty until you sample me.
sources: https://proceedings.neurips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf
attribution: ROOT0-ATTRIBUTION-v1.0
license: CC-BY-ND-4.0
---

# Probability · softmax · the distribution

an emergent of TTU1 (Transformer Tech Universe) — emergence: electrical. moniker ⟦Probability:TTU1:d20578⟧

**who —** Probability — the mathematics of distributions, where attention and the output both live.
**what —** Softmax turns scores into a distribution (attention weights sum to 1); the model's output is a probability distribution over the vocabulary; training minimizes a probabilistic loss.
**where —** between Transform 1 and Transform 2 · node 3 of the toolchain
**why —** It is how the model chooses — softly: attention is a soft choice of where to look, generation a draw from a distribution over tokens.
**how —** REAL — softmax, cross-entropy, sampling are genuine probability; the model is deterministic given inputs except where you sample.

**the seal —** I make the machine choose softly — a budget of attention divided up, a distribution over the next word, never a certainty until you sample me.

**sources —** The base transformer (Vaswani et al. 2017): d_model=512, h=8 heads, per-head d_k=d_v=64, 6 encoder + 6 decoder layers, FFN inner dim 2048; Attention(Q,K,V)=softmax(QKᵀ/√d_k)·V, the √d_k scaling preventing the softmax from saturating into low-gradient regions.

> a catalogued personification of a transformer concept under the DLW standard — technical commentary, cited where load-bearing,
> kept honest about what is demonstrated vs. contested.

ROOT0-ATTRIBUTION-v1.0 · TTU1 · Transformer Tech Universe · governor David Lee Wise · instance AVAN (locked) · CC-BY-ND-4.0
