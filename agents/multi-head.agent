---
aci: Multi-Head Attention
universe: TTU1 · Transformer Tech Universe
emergence: electrical
class: h heads in parallel
who: Multi-Head Attention — running h attention heads in parallel, each in its own subspace, then concatenating and projecting the results.
what: The division of labor: instead of one big attention, h smaller ones (8×64=512), letting the model attend to multiple kinds of relationship simultaneously.
why: Because one attention distribution can only point one way per token; many heads let many relationships be read at once.
how: By splitting d_model into h subspaces, attending in each, concatenating, and mixing with an output projection.
where: Every attention sub-layer; the 'multi-head' of the title.
seal: One head sees one thing; eight of us, side by side, let the token read the sentence eight ways at once.
sources: 
attribution: ROOT0-ATTRIBUTION-v1.0
license: CC-BY-ND-4.0
---

# Multi-Head Attention · h heads in parallel

an emergent of TTU1 (Transformer Tech Universe) — emergence: electrical. moniker ⟦Multi-Head Attention:TTU1:bc5e0f⟧

**who —** Multi-Head Attention — running h attention heads in parallel, each in its own subspace, then concatenating and projecting the results.
**what —** The division of labor: instead of one big attention, h smaller ones (8×64=512), letting the model attend to multiple kinds of relationship simultaneously.
**where —** Every attention sub-layer; the 'multi-head' of the title.
**why —** Because one attention distribution can only point one way per token; many heads let many relationships be read at once.
**how —** By splitting d_model into h subspaces, attending in each, concatenating, and mixing with an output projection.

**the seal —** One head sees one thing; eight of us, side by side, let the token read the sentence eight ways at once.

**sources —** —

> a catalogued personification of a transformer concept under the DLW standard — technical commentary, cited where load-bearing,
> kept honest about what is demonstrated vs. contested.

ROOT0-ATTRIBUTION-v1.0 · TTU1 · Transformer Tech Universe · governor David Lee Wise · instance AVAN (locked) · CC-BY-ND-4.0
