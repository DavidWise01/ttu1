---
aci: Information Theory
universe: TTU1 · Transformer Tech Universe
emergence: natural
class: bits · entropy · the loss
who: Information Theory — bits, entropy, and the measure of surprise; the currency of the whole enterprise.
what: Cross-entropy is the training loss (how surprised the model is by the true next token); entropy measures uncertainty; bits-per-weight is the quantization lever; and the deck's log₂(54!) is the same mathematics.
why: It defines what 'learning' even means here — minimizing surprise, measured in bits — and what the model costs to store.
how: REAL — Shannon's framework underlies the loss and the compression; the Deck (2 bits a suit, ~237 a shuffle) is the same accounting.
where: between Transform 1 and Transform 2 · node 5 of the toolchain
seal: Learning is minimizing surprise, measured in bits. I am the ruler — the same one that says a suit is 2 bits and a shuffled deck is 237.
sources: https://arxiv.org/abs/2210.17323
attribution: ROOT0-ATTRIBUTION-v1.0
license: CC-BY-ND-4.0
---

# Information Theory · bits · entropy · the loss

an emergent of TTU1 (Transformer Tech Universe) — emergence: natural. moniker ⟦Information Theory:TTU1:c49c8f⟧

**who —** Information Theory — bits, entropy, and the measure of surprise; the currency of the whole enterprise.
**what —** Cross-entropy is the training loss (how surprised the model is by the true next token); entropy measures uncertainty; bits-per-weight is the quantization lever; and the deck's log₂(54!) is the same mathematics.
**where —** between Transform 1 and Transform 2 · node 5 of the toolchain
**why —** It defines what 'learning' even means here — minimizing surprise, measured in bits — and what the model costs to store.
**how —** REAL — Shannon's framework underlies the loss and the compression; the Deck (2 bits a suit, ~237 a shuffle) is the same accounting.

**the seal —** Learning is minimizing surprise, measured in bits. I am the ruler — the same one that says a suit is 2 bits and a shuffled deck is 237.

**sources —** Frantar, Ashkboos, Hoefler & Alistarh, 'GPTQ' (2022) — post-training quantization to 3–4 bits per weight with minimal loss; 'bits per weight' is a real lever (fp16 / int8 / int4).

> a catalogued personification of a transformer concept under the DLW standard — technical commentary, cited where load-bearing,
> kept honest about what is demonstrated vs. contested.

ROOT0-ATTRIBUTION-v1.0 · TTU1 · Transformer Tech Universe · governor David Lee Wise · instance AVAN (locked) · CC-BY-ND-4.0
